# Convex (auto-generated by `bunx convex dev`)
CONVEX_DEPLOYMENT=dev:your-deployment-name
CONVEX_URL=https://your-deployment.convex.cloud

# WXT public env vars (available in extension bundle)
# These use VITE_ prefix and are available via import.meta.env
VITE_CONVEX_URL=https://your-deployment.convex.cloud
VITE_STRIPE_PRICE_ID=price_xxx

# Backend API URL (for extension to call FastAPI backend)
VITE_API_URL=http://localhost:8000

# ============================================
# CONVEX ENVIRONMENT VARIABLES
# Set these via: bunx convex env set KEY value
# ============================================

# Stripe (for payments)
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx

# AI APIs (REQUIRED for image enhancement)
# OPENAI_API_KEY=sk-xxx          # Get from: https://platform.openai.com/api-keys
# REPLICATE_API_KEY=r8_xxx       # Get from: https://replicate.com/account/api-tokens

# ============================================
# BACKEND ENVIRONMENT VARIABLES (backend/.env)
# ============================================
# REPLICATE_API_TOKEN=r8_xxx     # Get from: https://replicate.com/account/api-tokens
